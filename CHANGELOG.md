# Changelog

## 3.2.12 — release

- Man page completa no deb e em `eazy --install` / `--uninstall`
- Legenda de cores na linha inferior do header (DIR/VID/AUD/IMG/ZIP/DOC + idx)
- `eazy --install` detecta GNOME e KDE e instala deps do ambiente
- Launcher em `/usr/share/applications` + refresh de menu (update-desktop-database / kbuildsycoca)
- Seleção global: qtd e bytes ao limpar; lista completa em F9
- Extensões de pesquisa com rótulo de categoria
- Edição de links da fila de downloads (Ctrl+B)
- Exportar notas; preferência IA (smithi); data/hora no status
- Cache de busca/listas; limpeza gradle/snap na manutenção
- Pacote `.deb` sugerindo konsole/dolphin/kate (KDE) e xdg-utils

## 3.2.11

- Correção da man page no pacote (antes era stub de 45 bytes)

## 3.2.10

- Enter em py/sh/html com menu estilo PDF
