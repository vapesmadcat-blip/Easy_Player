#!/usr/bin/env bash
# eazy — o script completo (~198KB) não cabe no push via API desta integração.
# Use a cópia local fornecida no chat, ou:
#   1. Baixe o arquivo "eazy" completo do anexo da conversa
#   2. chmod +x eazy && ./eazy
#
# Patches inclusos na versão completa:
#   - Filtro (FZF_QUERY) persiste até mudar de pasta ou apagar
#   - Seleção persiste em SELECTED_FILE até Ctrl-X ou mudar de pasta
#   - Ctrl-A não inclui ".."
#   - eazy_sel_select_all / clear / invert
echo "eazy: baixe o script completo (anexo da conversa ou artifacts/Easy_Player/eazy)" >&2
echo "  chmod +x eazy && ./eazy" >&2
exit 1
